{% macro renderRoom(room) -%}
<tr class="normal mmm-hadisplay-room">
  <td class="align-left">{{ room.name }}</td>
  <td class="align-right">
    <span class="{{ room.temperature.valueClass }}">{{ room.temperature.value }}</span>
  </td>
  <td class="align-right">
    <span class="{{ room.humidity.valueClass }}">{{ room.humidity.value }}</span>
  </td>
  <td class="align-right">{{ room.pm25 }}</td>
  <td class="align-right mmm-hadisplay-condition">
    <span class="{{ room.lighting.iconClass }} fa-fw fa-lightbulb mmm-hadisplay-light {{ room.lighting.className }}" style="{{ room.lighting.style }}" aria-hidden="true"></span>
  </td>
  <td class="align-right mmm-hadisplay-condition">
    <span class="fas fa-fw {{ room.security.iconName }} mmm-hadisplay-security {{ room.security.className }}" role="img" aria-label="{{ room.security.label }}" title="{{ room.security.label }}"></span>
  </td>
</tr>
{%- endmacro %}
<div class="mmm-hadisplay">
  {% if loading %}
    <div class="small dimmed mmm-hadisplay-status">Loading…</div>
  {% elif initialError %}
    <div class="small dimmed mmm-hadisplay-status">Home Assistant data unavailable</div>
  {% else %}
    <table class="small mmm-hadisplay-table">
      <tbody>
        {% for floor in floors %}
          <tr class="dateheader mmm-hadisplay-section">
            <td colspan="6" class="bright">{{ floor.name }}</td>
          </tr>
          {% for room in floor.rooms %}
            {{ renderRoom(room) }}
          {% endfor %}
        {% endfor %}
        {% if otherRooms | length > 0 %}
          <tr class="dateheader mmm-hadisplay-section">
            <td colspan="6" class="bright">Other areas</td>
          </tr>
          {% for room in otherRooms %}
            {{ renderRoom(room) }}
          {% endfor %}
        {% endif %}
      </tbody>
    </table>
    {% if stale %}
      <div class="xsmall dimmed mmm-hadisplay-stale">Data unavailable</div>
    {% endif %}
  {% endif %}
</div>
